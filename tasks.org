* DONE don't refresh marks while interacting with the mark form
  CLOSED: [2026-03-16 Mon 14:30]
  Updated `use-app-side-effects` in `core.cljs` to skip fetching when `show-form`
  or `editing-mark` is active.
* DONE reduce size of notification of "refreshing marks"
  CLOSED: [2026-03-16 Mon 14:30]
  Replaced the success notification with a `:loading-marks?` state in `state.cljs`
  and a subtle `loading-indicator` spinner in the top-right corner of the app.
* DONE don't refresh marks for small movements of the map
  CLOSED: [2026-03-16 Mon 14:30]
  Implemented a 100km threshold for map movement in `core.cljs`.
  Marks are only re-fetched if the map center moves more than this distance
  from the last fetch location, or if settings change.
* DONE name is optional on a mark
  CLOSED: [2026-03-16 Mon 14:55]
  Updated `MarkSchema` in `common/src/com/hjsoft/mapmarks/common/logic.cljc`
  to make `:name` an optional `[:maybe :string]`.
  The UI already handled missing names gracefully.
  Added a test case for nameless marks in `mark_test.cljs`.
