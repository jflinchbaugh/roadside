* DONE don't refresh stands while interacting with the stand form
  CLOSED: [2026-03-16 Mon 14:30]
  Updated `use-app-side-effects` in `core.cljs` to skip fetching when `show-form`
  or `editing-stand` is active.
* DONE reduce size of notification of "refreshing stands"
  CLOSED: [2026-03-16 Mon 14:30]
  Replaced the success notification with a `:loading-stands?` state in `state.cljs`
  and a subtle `loading-indicator` spinner in the top-right corner of the app.
* DONE don't refresh stands for small movements of the map
  CLOSED: [2026-03-16 Mon 14:30]
  Implemented a 100km threshold for map movement in `core.cljs`.
  Stands are only re-fetched if the map center moves more than this distance
  from the last fetch location, or if settings change.
* DONE name is optional on a stand
  CLOSED: [2026-03-16 Mon 14:55]
  Updated `StandSchema` in `common/src/com/hjsoft/roadside/common/logic.cljc`
  to make `:name` an optional `[:maybe :string]`.
  The UI already handled missing names gracefully.
  Added a test case for nameless stands in `stand_test.cljs`.
